scenedetect
ultralytics
torch
torchvision
tqdm
# Nightly, not stable: stable 2026.07.04 downloads via the android_vr client,
# which YouTube answers with 403 mid-stream (yt-dlp issue #17456). The exact
# pin keeps Docker/CI reproducible. Raise it deliberately once a tested stable
# release contains the fix.
yt-dlp[default]==2026.8.18.122307.dev0
faster-whisper
google-genai
python-dotenv
mediapipe>=0.10.14
boto3

fastapi
uvicorn
python-multipart
httpx
Pillow
beautifulsoup4
